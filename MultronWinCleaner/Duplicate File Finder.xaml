<Window x:Class="MultronWinCleaner.Duplicate_File_Finder"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MultronWinCleaner"
        Title="Multron Duplicate File Finder"
        Height="760" Width="980"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        Background="Transparent">

    <Window.Resources>
 
        <Style x:Key="BigCircularButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Heavy"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="Height" Value="140"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="70" BorderThickness="4" BorderBrush="#22FFFFFF">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.25" BlurRadius="20" Direction="270" ShadowDepth="5"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#005A9E"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.02" ScaleY="1.02" CenterX="70" CenterY="70"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95" CenterX="70" CenterY="70"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#555555"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style> 
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource TextBoxBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
 
    <Border Background="{DynamicResource WindowBackground}" CornerRadius="12" BorderBrush="#33000000" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="25" ShadowDepth="0"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
 
            <Border Grid.Row="0" Background="{DynamicResource PanelBackground}" CornerRadius="12,12,0,0" MouseDown="TopBar_MouseDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0,0,0">
                      
                        <TextBlock Text="Multron Duplicate File Finder" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Opacity="0.8"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Button Content="—" Style="{StaticResource ModernButtonStyle}" Width="36" Height="30" Click="MinimizeButton_Click" ToolTip="Minimize" Margin="0,0,4,0"/>
                        <Button x:Name="buttonMaximize" Content="☐" Style="{StaticResource ModernButtonStyle}" Width="36" Height="30" FontSize="16" Click="MaximizeButton_Click" ToolTip="Maximize/Restore" Margin="0,0,4,0"/>
                        <Button Content="✕" Style="{StaticResource DangerButtonStyle}" Width="36" Height="30" Click="CloseButton_Click" ToolTip="Close"/>
                    </StackPanel>
                </Grid>
            </Border>
 
            <Grid Grid.Row="1" Margin="25,15,25,25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
 
                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,10,0,15">
                    <Button x:Name="MainActionButton" Content="SCAN" Style="{StaticResource BigCircularButton}" Click="MainActionButton_Click"/>

                    <TextBlock x:Name="StatusText" HorizontalAlignment="Center" Margin="0,15,0,5" FontSize="14" Foreground="{DynamicResource SecondaryText}">
                        <Run Text="Ready to scan"/><LineBreak/><Run/>
                    </TextBlock>

                    <TextBlock x:Name="TotalSizeText" Text="Recoverable Space: 0 MB" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource AccentColor}" Visibility="Collapsed"/>
                    <TextBlock x:Name="FilesScannedText" Text="" HorizontalAlignment="Center" FontSize="14" Opacity="0.7" Foreground="{DynamicResource SecondaryText}"/>
                </StackPanel>
  
                <StackPanel Grid.Row="1" Margin="0,0,0,15">
                    <TextBlock x:Name="AdvancedToggleText" Text="▼ Properties" HorizontalAlignment="Center" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource AccentColor}" Cursor="Hand" MouseLeftButtonUp="AdvancedToggle_MouseLeftButtonUp" Margin="0,0,0,10">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.8"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <Border x:Name="AdvancedPanel" Visibility="Collapsed" Background="{DynamicResource PanelBackground}" CornerRadius="10" Padding="20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="5*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="6*"/>
                            </Grid.ColumnDefinitions>
 
                            <StackPanel Grid.Column="0" Margin="0,0,15,0">
                                <TextBlock Text="Target Directories" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10"/>
                                <ListBox x:Name="FolderListBox" Height="120" Background="{DynamicResource TextBoxBackground}" BorderBrush="{DynamicResource BorderBrush}" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10">
                                    <ListBox.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </ListBox.Resources>
                                </ListBox>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="6"/>
                                          
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="6"/>
                                          
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <Button Grid.Column="0" Content="+ Add Folder" Click="BrowseButton_Click" Style="{DynamicResource RoundedButtonStyle}" Height="30"/>
                                        <Button Grid.Column="2" Content="+ C:\ Drive" Click="BtnAddDefault_Click" Style="{DynamicResource RoundedButtonStyle}" Height="30"/>
                                        <Button Grid.Column="4" Content="Remove" Click="RemoveButton_Click" Style="{DynamicResource DangerButtonStyle}" Height="30"/>
                                    </Grid>

                                </Grid>
                            </StackPanel>

                         
                            <Border Grid.Column="1" Width="1" Background="{DynamicResource SeparatorBrush}" Margin="10,5,25,5" Opacity="0.5"/>
 
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Scan Filters &amp; Data Options" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10"/>

                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="Min Size (KB):" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}" Margin="0,0,8,0"/>
                                    <TextBox Grid.Column="1" x:Name="MinSizeTextBox" Text="1024" Style="{StaticResource ModernTextBox}" Height="30" Margin="0,0,12,0" PreviewTextInput="NumberValidationTextBox" ToolTip="Files smaller than this will be ignored (e.g. 1024 for 1MB)."/>

                                    <TextBlock Grid.Column="2" Text="Preset:" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}" Margin="0,0,8,0"/>
                                    <ComboBox Grid.Column="3" x:Name="ExtensionPresetComboBox" Height="30" SelectionChanged="ExtensionPresetComboBox_SelectionChanged">
                                        <ComboBoxItem Content="All Files (*.*)"/>
                                        <ComboBoxItem Content="Images Only"/>
                                        <ComboBoxItem Content="Videos Only"/>
                                        <ComboBoxItem x:Name="CustomPresetItem" Content="Custom" IsSelected="True"/>
                                    </ComboBox>
                                </Grid>
 
                                <Grid Margin="0,0,0,10">
                                    <Grid.Style>
                                        <Style TargetType="Grid">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=CustomPresetItem, Path=IsSelected}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Extension Filter:" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimary}" Margin="0,0,8,0"/>
                                    <TextBox Grid.Column="1" x:Name="ExtensionTextBox" Text="*.*" Style="{StaticResource ModernTextBox}" Height="30" ToolTip="Example: *.mp4, *.jpg or *.* for all files"/>
                                </Grid>

                             
                            
                                <GroupBox Header="Quick Extension Select" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10" Padding="5">
                                    <GroupBox.Style>
                                        <Style TargetType="GroupBox">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=CustomPresetItem, Path=IsSelected}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </GroupBox.Style>
                                    <UniformGrid Columns="4">
                                        <CheckBox x:Name="PngCheckBox" Content=".png" IsChecked="True" Foreground="{DynamicResource TextPrimary}" Margin="2" Checked="ExtensionCheckBox_CheckedChanged" Unchecked="ExtensionCheckBox_CheckedChanged"/>
                                        <CheckBox x:Name="JpegCheckBox" Content=".jpeg" IsChecked="True" Foreground="{DynamicResource TextPrimary}" Margin="2" Checked="ExtensionCheckBox_CheckedChanged" Unchecked="ExtensionCheckBox_CheckedChanged"/>
                                        <CheckBox x:Name="JpgCheckBox" Content=".jpg" IsChecked="True" Foreground="{DynamicResource TextPrimary}" Margin="2" Checked="ExtensionCheckBox_CheckedChanged" Unchecked="ExtensionCheckBox_CheckedChanged"/>
                                        <CheckBox x:Name="WebpCheckBox" Content=".webp" IsChecked="True" Foreground="{DynamicResource TextPrimary}" Margin="2" Checked="ExtensionCheckBox_CheckedChanged" Unchecked="ExtensionCheckBox_CheckedChanged"/>
                                        <CheckBox x:Name="Mp4CheckBox" Content=".mp4" IsChecked="True" Foreground="{DynamicResource TextPrimary}" Margin="2" Checked="ExtensionCheckBox_CheckedChanged" Unchecked="ExtensionCheckBox_CheckedChanged"/>
                                        <CheckBox x:Name="AviCheckBox" Content=".avi" IsChecked="True" Foreground="{DynamicResource TextPrimary}" Margin="2" Checked="ExtensionCheckBox_CheckedChanged" Unchecked="ExtensionCheckBox_CheckedChanged"/>
                                        <CheckBox x:Name="MkvCheckBox" Content=".mkv" IsChecked="True" Foreground="{DynamicResource TextPrimary}" Margin="2" Checked="ExtensionCheckBox_CheckedChanged" Unchecked="ExtensionCheckBox_CheckedChanged"/>
                                        <CheckBox x:Name="MovCheckBox" Content=".mov" IsChecked="True" Foreground="{DynamicResource TextPrimary}" Margin="2" Checked="ExtensionCheckBox_CheckedChanged" Unchecked="ExtensionCheckBox_CheckedChanged"/>
                                    </UniformGrid>
                                </GroupBox>

                                <CheckBox x:Name="CheckQualityCheckBox" Content="Match media across different qualities/resolutions" IsChecked="True" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,6" ToolTip="If enabled, finds videos/images with the same name but different resolutions."/>
                                <CheckBox x:Name="ShowHashCheckBox" Content="Display SHA256 Hash in results tree" IsChecked="True" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,12" Checked="ShowHashCheckBox_Checked" Unchecked="ShowHashCheckBox_Unchecked"/>

                                <Grid Margin="0,10,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="12"/>
                                    
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Column="0" Content="Open Duplicates.json" Click="BtnOpenJson_Click" Style="{DynamicResource RoundedButtonStyle}" Height="34" ToolTip="Opens the folder/file where scan results are saved."/>
                                    <Button Grid.Column="2" Content="Reset Findings" Click="BtnDeleteJson_Click" Style="{DynamicResource DangerButtonStyle}" Height="34" ToolTip="Permanently deletes the saved duplicates cache file."/>
                                </Grid>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>
 
                <Grid Grid.Row="2" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="{DynamicResource TextBoxBackground}" CornerRadius="20" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Height="36">
                        <Grid>
                            <TextBlock Text="🔍" VerticalAlignment="Center" Margin="12,0,0,0" Opacity="0.5" Foreground="{DynamicResource TextPrimary}"/>
                            <TextBox x:Name="SearchBox" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextPrimary}" VerticalContentAlignment="Center" Padding="35,0,15,0" TextChanged="SearchBox_TextChanged" />
                            <TextBlock Text="Search files or folders..." IsHitTestVisible="False" VerticalAlignment="Center" Margin="38,0,0,0" Foreground="{DynamicResource SecondaryText}" Opacity="0.6">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text.Length, ElementName=SearchBox}" Value="0">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Border>

                    <TextBlock x:Name="SearchCountText" Grid.Column="1" Text="" VerticalAlignment="Center" Foreground="{DynamicResource SecondaryText}" FontWeight="SemiBold" Margin="15,0,0,0"/>
                </Grid>
 
                <Border Grid.Row="3" Background="{DynamicResource TextBoxBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="10">
                    <Grid>
                        <TreeView x:Name="DuplicatesTreeView" 
                                  VirtualizingStackPanel.IsVirtualizing="True" 
                                  VirtualizingStackPanel.VirtualizationMode="Recycling" 
                                  Background="Transparent" 
                                  BorderThickness="0" 
                                  Foreground="{DynamicResource TextPrimary}">

                            <TreeView.Resources>
                            
                                <HierarchicalDataTemplate DataType="{x:Type local:FolderNodeModel}" ItemsSource="{Binding Children}">
                                    <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                                        <StackPanel.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="📁 Open Folder" Tag="{Binding FullPath}" Click="ContextMenu_OpenFolder_Click"/>
                                            </ContextMenu>
                                        </StackPanel.ContextMenu>

                                        <TextBlock Text="📁" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>

                                        <Border Background="{DynamicResource AccentColor}" CornerRadius="12" Margin="12,0,0,0" Padding="8,2" VerticalAlignment="Center" Cursor="Hand" Tag="{Binding}" MouseLeftButtonUp="FolderBadge_Click" ToolTip="Click to view every duplicate file inside this folder">
                                            <TextBlock Text="{Binding TotalDuplicateCount}" FontSize="12" Foreground="White" FontWeight="Bold"/>
                                        </Border>
                                    </StackPanel>
                                </HierarchicalDataTemplate>
 
                                <DataTemplate DataType="{x:Type local:FileNodeModel}">
                                    <Border Margin="0,4" CornerRadius="8" Padding="12,8" Background="{DynamicResource PanelBackground}" BorderBrush="#11000000" BorderThickness="1">
                                        <Border.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="📄 Open File" Tag="{Binding FilePath}" Click="ContextMenu_OpenFile_Click"/>
                                                <MenuItem Header="📂 Open File Location" Tag="{Binding FilePath}" Click="ContextMenu_OpenFileLocation_Click"/>
                                                <Separator/>
                                                <MenuItem Header="🔍 Show Matching Files" Tag="{Binding}" Click="ContextMenu_ShowMatchingFiles_Click" IsEnabled="{Binding HasMatches}"/>
                                            </ContextMenu>
                                        </Border.ContextMenu>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <CheckBox Grid.Column="0" IsChecked="{Binding IsSelectedForDeletion}" VerticalAlignment="Center" Margin="0,0,12,0" Cursor="Hand">
                                                <CheckBox.LayoutTransform>
                                                    <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                                                </CheckBox.LayoutTransform>
                                            </CheckBox>

                                            <TextBlock Grid.Column="1" Text="{Binding FileIcon}" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>

                                            <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,0,15,0">
                                                <TextBlock Text="{Binding FileName}" Foreground="{DynamicResource TextPrimary}" FontSize="13" TextTrimming="CharacterEllipsis"/>

                                                <TextBlock Cursor="Hand" MouseLeftButtonUp="MatchSummary_Click" Tag="{Binding}" Margin="0,2,0,0">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding HasMatches}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                    <Run Text="🔗 " Foreground="{DynamicResource AccentColor}"/>
                                                    <Run Text="{Binding MatchSummary, Mode=OneWay}" Foreground="{DynamicResource AccentColor}" TextDecorations="Underline" FontSize="11"/>
                                                </TextBlock>

                                                <TextBlock Text="{Binding Hash}" FontSize="11" FontFamily="Consolas" Foreground="{DynamicResource SecondaryText}" TextTrimming="CharacterEllipsis" Margin="0,2,0,0">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsHashVisible}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </StackPanel>

                                            <Border Grid.Column="3" Background="{DynamicResource AccentColor}" Opacity="0.9" CornerRadius="6" Padding="8,4" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding FormattedSize}" FontSize="12" Foreground="White" FontWeight="SemiBold"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </TreeView.Resources>

                            <TreeView.ItemContainerStyle>
                                <Style TargetType="TreeViewItem">
                                    <Setter Property="IsExpanded" Value="False"/>
                                    <Setter Property="Padding" Value="6,4"/>
                                    <Setter Property="Margin" Value="0,2"/>
                                </Style>
                            </TreeView.ItemContainerStyle>
                        </TreeView>

                      
                        <Border x:Name="LoadingOverlay" Background="#CC000000" Visibility="Collapsed" Panel.ZIndex="10" CornerRadius="10">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ProgressBar IsIndeterminate="True" Width="220" Height="6" Margin="0,0,0,20" Foreground="{DynamicResource AccentColor}" BorderThickness="0" Background="#33FFFFFF"/>
                                <TextBlock Text="Loading previous scan, please wait..." Foreground="White" FontSize="15" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>