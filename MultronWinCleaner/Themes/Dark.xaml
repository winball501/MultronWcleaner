<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

   
    <Color x:Key="DropShadowColor">#88000000</Color>
    <Color x:Key="DangerColor">#B61423</Color>
    <SolidColorBrush x:Key="AccentColor">#FF2A9DF4</SolidColorBrush>
    <SolidColorBrush x:Key="ToggleButtonBackground" Color="#FF3A3A3A"/>
    <SolidColorBrush x:Key="ToggleButtonForeground" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="ToggleButtonBorder" Color="#FF5A5A5A"/>
    <SolidColorBrush x:Key="ButtonBorder" Color="#FF005A9E"/>
    <SolidColorBrush x:Key="AccentColorHover" Color="#1E7DD7" />
    <SolidColorBrush x:Key="AccentColorPressed" Color="#1664B3" />
    <SolidColorBrush x:Key="AccentBrush" Color="#FF4A90E2" />
    <SolidColorBrush x:Key="TextBoxForeground" Color="#FF222222" />
    <SolidColorBrush x:Key="TextBoxBorderBrush" Color="#FFAAAAAA" />
    <SolidColorBrush x:Key="ExceptionListBackground" Color="#FFF9F9FC" />
    <SolidColorBrush x:Key="ExceptionListForeground" Color="#FF222222" />
    <SolidColorBrush x:Key="WindowBorder" Color="#FF3C3C3C"/>
    <SolidColorBrush x:Key="TitleBarBackground" Color="#FF252526"/>
    <SolidColorBrush x:Key="TitleBarText" Color="#FFFFFFFF"/>
    <Style x:Key="RoundedButton" TargetType="Button">
        <Setter Property="Background" Value="#3B82F6"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="8">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2563EB"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#1D4ED8"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CustomCheckBoxStyle" TargetType="CheckBox">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Margin" Value="4,2"/>
        <Setter Property="Padding" Value="6,2"/>
    </Style>
    <SolidColorBrush x:Key="ButtonIconForeground" Color="#BBBBBB"/>
 
    <SolidColorBrush x:Key="ComboBoxBackground" Color="#2D2D30"/>
 
    <SolidColorBrush x:Key="ListViewBackground" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="ProgressBarForeground" Color="#0A84FF"/>

    <Style TargetType="ComboBox" x:Key="DarkComboBoxStyle">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#121212"/>
        <Setter Property="BorderBrush" Value="#3A3A3C"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6,3"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton
            Name="ToggleButton"
            Grid.Column="2"
            Focusable="false"
            IsChecked="{Binding Path=IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
            ClickMode="Press"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}">
                            <Path
              x:Name="Arrow"
              HorizontalAlignment="Center"
              VerticalAlignment="Center"
              Data="M 0 0 L 4 4 L 8 0 Z"
              Fill="White"/>
                        </ToggleButton>

                        <ContentPresenter
            Name="ContentSite"
            IsHitTestVisible="False"
            Content="{TemplateBinding SelectionBoxItem}"
            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
            Margin="6,3,25,3"
            VerticalAlignment="Center"
            HorizontalAlignment="Left" />

                        <Popup
            Name="Popup"
            Placement="Bottom"
            IsOpen="{TemplateBinding IsDropDownOpen}"
            AllowsTransparency="True"
            Focusable="False"
            PopupAnimation="Fade">
                            <Grid
              Name="DropDown"
              SnapsToDevicePixels="True"
              MinWidth="{TemplateBinding ActualWidth}"
              MaxHeight="200"
              Background="#121212">
                                <Border
                x:Name="DropDownBorder"
                BorderThickness="1"
                BorderBrush="#3A3A3C"
                Background="#121212" />
                                <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Grid>
                        </Popup>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="false">
                            <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="#888"/>
                            <Setter TargetName="Arrow" Property="Fill" Value="#888"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="#555"/>
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter Property="BorderBrush" Value="#3399FF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="RoundedTextBox" TargetType="TextBox">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="Background" Value="#1E1E1E"/>
       
        <Setter Property="Foreground" Value="White"/>
       
        <Setter Property="BorderBrush" Value="#3A3A3C"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="8">
                        <ScrollViewer x:Name="PART_ContentHost" Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="AccentButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource AccentColor}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Height" Value="35" />
        <Setter Property="Margin" Value="0,0,0,10" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0" CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentColorHover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentColorPressed}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="#FF888888" />
                           
                            <Setter Property="Foreground" Value="#FFCCCCCC" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CancelButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource ButtonCancelBackground}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Width" Value="206"/>
        <Setter Property="Height" Value="37"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0" CornerRadius="6" >
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="#FFB0B0B0" />
                           
                            <Setter Property="Foreground" Value="#FFE0E0E0" />
                          
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#FFCC4444"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#FFAA2222"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Color x:Key="TextColor">#FFFFFFFF</Color>
 
    <SolidColorBrush x:Key="ButtonNormalBackground" Color="#007ACC" />
    <SolidColorBrush x:Key="DangerButtonBackground" Color="#D9534F" />
    <Color x:Key="SecondaryTextColor">#FFAAAAAA</Color>
    <SolidColorBrush x:Key="ButtonForeground" Color="#D7DADC"/>
    <SolidColorBrush x:Key="ButtonBackground" Color="#2A2A2E" />
    <SolidColorBrush x:Key="ButtonHoverForeground" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="ButtonPressedBackground" Color="#5A5F73"/>
    <SolidColorBrush x:Key="ButtonPressedForeground" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="ButtonCancelBackground" Color="#6c757d"/>
    <SolidColorBrush x:Key="ProgressBarBackground" Color="#2C2C2C"/>
    <SolidColorBrush x:Key="ProgressBarBorder" Color="#444444"/>

    <SolidColorBrush x:Key="TextPrimary" Color="#D7DADC"/>
    <SolidColorBrush x:Key="TextSecondary" Color="#8E8E93"/>
 
    
    <SolidColorBrush x:Key="Text" Color="{StaticResource TextColor}"/>
    <SolidColorBrush x:Key="SecondaryText" Color="{StaticResource SecondaryTextColor}"/>
   
    <SolidColorBrush x:Key="WindowBackground" Color="#FF121212"/>
    <SolidColorBrush x:Key="PanelBackground" Color="#FF1E1E1E"/>
    <SolidColorBrush x:Key="ButtonHoverBackground" Color="#FF2A2A2A"/>
    <SolidColorBrush x:Key="SeparatorBrush" Color="#FF444444"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#FF444444"/>
    <SolidColorBrush x:Key="TextBoxBackground" Color="#FF2B2B2B"/>
    <SolidColorBrush x:Key="DataGridBackground" Color="#FF1E1E1E"/>
    <SolidColorBrush x:Key="DataGridRowBackground" Color="#FF292929"/>
    <SolidColorBrush x:Key="DataGridAlternatingRowBackground" Color="#FF242424"/>
 
    <DropShadowEffect x:Key="DropShadowEffect" Color="{StaticResource DropShadowColor}" BlurRadius="12" ShadowDepth="5" Opacity="0.5"/>
    <Style x:Key="ModernButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource ButtonForeground}" />
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="10,5"/>
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Border x:Name="shadow"
                            Background="Transparent"
                            CornerRadius="6"
                            Margin="2">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="15" ShadowDepth="3" Opacity="0.3" />
                            </Border.Effect>
                        </Border>

                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource ButtonHoverBackground}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="RoundedButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#2A9DF4"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                            CornerRadius="8" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FF1E86D4"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#FF176BB0"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="#FF555555"/>
                            <Setter Property="Foreground" Value="#FFAAAAAA"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource RoundedButtonStyle}">
        <Setter Property="Background" Value="DarkRed"/>
        <Setter Property="Foreground" Value="White"/>
    </Style>
 
    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="#444"/>
        <Setter Property="Foreground" Value="#DDDDDD"/>
        <Setter Property="BorderBrush" Value="#555"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
    <Style TargetType="DataGridCell">
        <Setter Property="Foreground" Value="#DDDDDD"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="#555"/>
    </Style>
    
    <SolidColorBrush x:Key="ButtonDangerBackground" Color="#FFB61423"/>
    <SolidColorBrush x:Key="DangerButtonHoverBackground" Color="#FF911820"/>
    <SolidColorBrush x:Key="DangerButtonPressedBackground" Color="#FF6B0F18"/>

    <SolidColorBrush x:Key="NotifyCleanedAmountForeground" Color="#5CD65C"/>
    <SolidColorBrush x:Key="DangerButtonForeground" Color="White"/>
 
    <SolidColorBrush x:Key="ListBoxBackground" Color="#FF252525"/>
    <SolidColorBrush x:Key="ListBoxForeground" Color="#FFE0E0E0"/>

  
</ResourceDictionary>
