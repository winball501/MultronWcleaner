<Window x:Class="MultronWinCleaner.AddStartup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Startup Application" Height="200" Width="400" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" WindowStyle="None" Background="{DynamicResource WindowBackground}" FontFamily="Segoe UI">

    <Window.Resources>
        
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#333"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>

      
        <Border Background="{DynamicResource PanelBackground}" Height="40" VerticalAlignment="Top">
            <Grid MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                
                <TextBlock Text="Add Startup Application" 
                           FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" 
                           VerticalAlignment="Center" Margin="12,0,0,0"
                           Grid.Column="0" />

                 
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0" Grid.Column="1">
                    <Button Content="✖" Width="32" Height="32" Style="{StaticResource TitleBarButtonStyle}"
                            Foreground="{DynamicResource DangerButtonBackground}" Click="CloseButton_Click" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

       
        <Grid Margin="10,40,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock Text="Name:"/>
                <TextBox x:Name="txtName" Margin="0,5,0,15"/>
                <TextBlock Text="Executable Path:"/>
                <TextBox x:Name="txtPath" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="Add" Width="80" Margin="5" IsDefault="True" Click="Add_Click"/>
            </StackPanel>
        </Grid>

    </Grid>
</Window>
