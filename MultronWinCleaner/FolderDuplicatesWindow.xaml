<Window x:Class="MultronWinCleaner.FolderDuplicatesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Folder Duplicates"
        Width="600" Height="520"
        MinWidth="420" MinHeight="300"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackground}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="44"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
 
        <DockPanel Grid.Row="0" Background="{DynamicResource PanelBackground}" LastChildFill="False" MouseDown="TitleBar_MouseDown">
            <TextBlock Text="Folder Duplicates" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}"
                       VerticalAlignment="Center" Margin="16,0,0,0" DockPanel.Dock="Left"/>
            <Button Content="✕" Width="30" Height="30" DockPanel.Dock="Right" Margin="0,0,8,0"
                    Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextPrimary}"
                    Cursor="Hand" Click="CloseButton_Click"/>
        </DockPanel>
 
        <TextBlock x:Name="HeaderText" Grid.Row="1" Margin="16,12,16,8"
                   FontSize="13" Foreground="{DynamicResource SecondaryText}" TextWrapping="Wrap"/>
 
        <ListBox x:Name="FilesListBox" Grid.Row="2" Margin="12,0,12,12"
                 Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 VirtualizingPanel.ScrollUnit="Pixel">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,4"/>
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
 
            <ListBox.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource PanelBackground}" CornerRadius="6" Padding="8,4" Margin="0,10,0,4">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontWeight="SemiBold" Foreground="{DynamicResource AccentColor}" FontSize="12">
                                        <Run Text="{Binding ItemCount, Mode=OneWay}"/>
                                        <Run Text=" identical copies"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding Items[0].FormattedSize, StringFormat=' · {0} each'}"
                                               FontSize="12" Foreground="{DynamicResource SecondaryText}" Margin="6,0,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListBox.GroupStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="6" Padding="10,8" Background="{DynamicResource TextBoxBackground}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding FileIcon}" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding FileName}" Foreground="{DynamicResource TextPrimary}" TextTrimming="CharacterEllipsis"/>
                                <TextBlock Text="{Binding FilePath}" FontSize="11" Foreground="{DynamicResource SecondaryText}" TextTrimming="CharacterEllipsis"/>
                            </StackPanel>

                            <Button Grid.Column="2" Content="📄" ToolTip="Open File" Tag="{Binding}" Click="OpenFile_Click"
                                    Width="30" Height="30" Margin="6,0,0,0" Background="Transparent" BorderThickness="0"
                                    Foreground="{DynamicResource TextPrimary}" Cursor="Hand" FontSize="14"/>

                            <Button Grid.Column="3" Content="📂" ToolTip="Open File Location" Tag="{Binding}" Click="OpenLocation_Click"
                                    Width="30" Height="30" Margin="4,0,0,0" Background="Transparent" BorderThickness="0"
                                    Foreground="{DynamicResource TextPrimary}" Cursor="Hand" FontSize="14"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
